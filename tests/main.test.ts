import { describe, expect, test } from '@jest/globals';

describe('remix-intl tests', () => {
  describe('utils tests', () => {
    expect(1 + 2).toBe(3);
  });

  describe('i18n.ts tests', () => {
    expect(1 + 2).toBe(3);
  });
});

describe('remix-intl e2e tests', () => {
  describe('normal tests', () => {
    test('open website, i18n works', async () => {
      //
    });

    test('navigate pages i18n works', async () => {
      //
    });

    test('no reload switch locale works', async () => {
      //
    });

    test('reload switch locale works', async () => {
      //
    });

    test('`meta` function works', async () => {
      //
    });

    test('`action` function works', async () => {
      //
    });
  });
});
